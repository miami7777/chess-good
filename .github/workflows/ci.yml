name: CG Deploy

on:
  push:
    branches:["main"]

jobs:
  build: 
  runs-on: ubuntu-latest  
    steps: 
      - name: Build Docker image
        run: docker build -t CG . 
        
          docker run --name CG -p 5144:8080 -d CG